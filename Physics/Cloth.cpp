#include "Cloth.h"

using namespace Crystal::Math;
using namespace Crystal::Physics;

Cloth::Cloth(const std::vector< std::vector< SPHParticle*> >& particles)
{
	/*
	for (int i = 1; i < particles.size(); ++i) {
		for (int j = 1; j < particles[i].size(); ++j) {
			Particle* p1 = particles[i-1][j];
			Particle* p2 = particles[i][j];
			Spring spring(p1, p2);
			springs.push_back()
		}
	}
	*/
}

void Cloth::coordinate()
{

}
